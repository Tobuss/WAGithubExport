{
  "type": "module",
  "dependencies": {
    "axios": "^0.24.0",
    "download": "^8.0.0"
  },
  "scripts": {
    "test": "export $(cat .env | xargs) && node main.js",
    "autobackup": "node main.js && git add -A && git commit -m \"autobackup-$(date '+%y%m%d')\" && git push"
  }
}
